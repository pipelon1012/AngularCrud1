Crud con angular y bootstrap, ingresa, lista, actualiza y elimina personas y su pais.
En la consola, ingreso a la carpeta e inicio el servidor con ng serve.
En el navegador le doy http://localhost:4200/.
